These instructions are typed by the user, not made by an AI agent, so the formatting may seem a bit off.

Please read the README.md before starting any projects to get the preferred tech stack.

Please remind the user to mount the `nginx-config` folder, so you can create custom Nginx configurations.

Licence is custom in the `LICENCE.md` file

Make sure to load the i-have-adhd and dyselxia-friendly-code skills if installed, else prompt the user to install them, and continue without them.